<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.ev_lib_2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="318b4b4d7731fdcf0e18d734411ac0fcde21bcf6" />

        <activity android:name=".BookDetails" />
        <activity android:name=".Mybooks" />
        <activity android:name=".ContactUS" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Login" />
        <activity android:name=".forgetPasswared" />
        <activity android:name=".regestrationActivity"></activity>
    </application>

</manifest>
